# Copiar a validador-frontend/.env en el VPS y completar ANTES de buildear
# (Vite hornea estas variables en el bundle en tiempo de build, docker-compose
# no las puede inyectar en runtime).

VITE_API_BASE_URL=https://mpvalidador.online/api
VITE_WS_BASE_URL=wss://mpvalidador.online
